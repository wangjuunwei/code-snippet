import styled from "styled-components/macro";

export const PicCapchaWrpper = styled.div`
  width: 100px;
  position: relative;

  svg {
    margin: 0 auto;
    position: absolute;
    top: calc(50% - 3px);
  }
`
